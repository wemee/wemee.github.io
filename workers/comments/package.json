{
  "name": "wemee-comments",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + D1 留言 API for wemee.github.io",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute wemee-comments --remote --file=schema.sql",
    "schema:local": "wrangler d1 execute wemee-comments --local --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.95.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "ws": "^8.20.2"
  }
}
