---
import BaseLayout from '@/layouts/BaseLayout.astro';
import '@/styles/pachinko.css';
---
<BaseLayout
  title="封入式柏青哥模擬器 | 挫屎勇的遊戲間"
  description="用程式模擬日本柏青哥（パチンコ）三大機種：デジパチ（1種 ST 甘デジ）、羽根物（2種）、1種2種混合機。內建狀態流程導覽，邊玩邊看懂通常時、初當り、RUSH／ST 的運作與長期回收率，純教育用途。"
  currentPage="game"
>
  <div class="w-full px-4 mt-6 mb-12">
    <header class="max-w-3xl mx-auto text-center mb-6">
      <h1 class="text-2xl md:text-3xl font-bold text-base-50">🎰 封入式柏青哥模擬器</h1>
      <p class="mt-3 text-base-300 leading-relaxed">
        用程式重現日本柏青哥三大機種的抽選與出玉機制——
        <strong class="text-base-100">デジパチ（1種 ST）</strong>、
        <strong class="text-base-100">羽根物（2種）</strong>、
        <strong class="text-base-100">1種2種混合機</strong>。
        右側流程導覽會即時標出你在哪個狀態，邊打邊看懂通常時、初當り、RUSH／ST 怎麼運作。
      </p>
      <p class="mt-3 text-sm text-accent-yellow/90">
        ⚠️ 純粹是機率與遊戲機制的教學模擬，無真實金錢、無法兌換。三台的長期回收率都校準在 100% 以下——
        「玩越久越虧」正是這個模擬要讓你親眼看見的事。
      </p>
      <p class="mt-4">
        <a
          href="/blog/pachinko-state-machine"
          class="inline-flex items-center px-3 py-1.5 text-sm rounded border border-accent-cyan text-accent-cyan hover:bg-accent-cyan hover:text-base-50 transition no-underline"
        >
          📝 背後的開發筆記：把柏青哥寫成一台狀態機<span class="ml-1">→</span>
        </a>
      </p>
    </header>

    <div class="pachinko">
      <div class="layout">
        <div class="app">
          <div class="topbar">
            <label for="machineSelect">機台</label>
            <select id="machineSelect"></select>
          </div>

          <div class="statusbar">
            <span id="phase" class="phase">通常時</span>
            <span id="direction" class="direction">← 左打</span>
          </div>

          <div class="board">
            <svg id="board-svg" viewBox="0 0 400 120" width="100%" role="img" aria-label="盤面示意"></svg>
            <div id="reels" class="reels">
              <div id="reel0" class="reel">0</div>
              <div id="reel1" class="reel">0</div>
              <div id="reel2" class="reel">0</div>
            </div>
          </div>

          <div id="stats" class="stats"></div>

          <div class="controls">
            <button id="shoot">發射一發</button>
            <button id="auto" class="secondary">自動發射</button>
          </div>
          <div class="controls">
            <button id="insert">投入 1000 円</button>
            <button id="reset" class="secondary">重置</button>
          </div>

          <div id="log" class="log"></div>
        </div>

        <aside class="guide">
          <div class="now">
            <div class="now-tag">現在</div>
            <h2 id="g-title">通常時</h2>
            <div class="now-row"><span>🎯 目標</span><p id="g-goal">—</p></div>
            <div class="now-row"><span>🎲 機率</span><p id="g-prob">—</p></div>
            <div class="now-row"><span>💡 說明</span><p id="g-hint">—</p></div>
          </div>

          <div class="flow">
            <div class="flow-title">狀態總覽（你在 ◀ 處）</div>
            <ol id="flow-list"></ol>
            <p id="flow-note" class="flow-note"></p>
          </div>
        </aside>
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  import { machines, initPachinko } from '@/lib/games/pachinko';
  initPachinko(machines);
</script>
