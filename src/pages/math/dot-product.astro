---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Navbar from '@/components/Navbar.astro';
---

<BaseLayout 
  title="向量內積視覺化 (Dot Product Visualizer)"
  description="直觀感受向量相似度、內積與投影的幾何意義"
>
  <Navbar currentPage="math" />
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-8 mb-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold text-base-50 mb-2">🏹 向量內積視覺化</h1>
      <p class="text-base-400">直觀感受向量相似度、內積與投影的幾何意義</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <!-- 左側：視覺化畫布 -->
      <div class="lg:col-span-2">
        <div class="rounded-lg border border-base-600 bg-base-900 overflow-hidden shadow-lg">
          <div class="relative">
            <canvas id="vectorCanvas" class="w-full cursor-crosshair" style="height: 500px;"></canvas>
            <div class="absolute bottom-3 left-3 text-base-600 text-sm">
              👆 拖曳箭頭端點以改變向量
            </div>
          </div>
        </div>
      </div>

      <!-- 右側：控制與數據 -->
      <div class="space-y-4">
        <!-- 控制面板 -->
        <div class="rounded-lg border border-base-600 bg-base-800 overflow-hidden">
          <div class="px-4 py-3 bg-base-600/30 border-b border-base-600 font-bold text-accent-blue">
            🎛️ 控制選項
          </div>
          <div class="p-4">
            <label class="flex items-center gap-3 cursor-pointer mb-4">
              <input type="checkbox" id="normalizeToggle" class="w-4 h-4 rounded border-base-600 bg-base-900 text-accent-blue">
              <span>
                <span class="block text-base-50">鎖定單位長度 (Normalize)</span>
                <span class="block text-sm text-base-600">排除長度影響，純觀察方向相似度</span>
              </span>
            </label>
            
            <p class="text-xs text-base-600 uppercase font-bold mb-2">快速預設</p>
            <div class="flex flex-wrap gap-2">
              <button class="preset-btn px-3 py-1 rounded border border-accent-cyan text-accent-cyan hover:bg-accent-cyan/10 text-sm transition" data-type="similar">✓ 相似 (0°)</button>
              <button class="preset-btn px-3 py-1 rounded border border-accent-yellow text-accent-yellow hover:bg-accent-yellow/10 text-sm transition" data-type="orthogonal">+ 正交 (90°)</button>
              <button class="preset-btn px-3 py-1 rounded border border-accent-red text-accent-red hover:bg-accent-red/10 text-sm transition" data-type="opposite">↔ 相反 (180°)</button>
              <button class="preset-btn px-3 py-1 rounded border border-base-600 text-base-400 hover:bg-base-600 text-sm transition" data-type="random">🎲 隨機</button>
            </div>
          </div>
        </div>

        <!-- 數學數值 -->
        <div class="rounded-lg border border-accent-blue bg-base-800 overflow-hidden">
          <div class="px-4 py-3 bg-accent-blue text-base-50 font-bold">
            📊 即時數據
          </div>
          <div class="p-4 font-mono">
            <!-- 內積 -->
            <div class="mb-4">
              <label class="text-base-600 text-sm">內積 (Dot Product)</label>
              <div class="text-3xl font-bold text-base-50" id="val-dot">0.00</div>
              <small class="text-base-600">
                <span class="text-accent-red">A</span> · <span class="text-accent-cyan">B</span> = |A| |B| cos(θ)
              </small>
            </div>
            
            <hr class="border-base-600 my-4">

            <!-- 相似度 -->
            <div class="mb-4">
              <label class="text-base-600 text-sm">餘弦相似度 (Cosine Similarity)</label>
              <div class="text-3xl font-bold text-accent-cyan" id="val-cos">0.000</div>
              <div class="h-1.5 bg-base-600 rounded mt-2 overflow-hidden">
                <div id="sim-bar" class="h-full bg-accent-cyan transition-all duration-300" style="width: 50%"></div>
              </div>
              <small class="text-base-600">純方向性 (-1 ~ 1)</small>
            </div>

            <!-- 角度 -->
            <div class="grid grid-cols-2 gap-3">
              <div>
                <label class="text-base-600 text-sm">夾角 (θ)</label>
                <div class="text-xl font-bold text-base-50" id="val-deg">0°</div>
              </div>
              <div class="text-right">
                <label class="text-base-600 text-sm">投影特性</label>
                <div class="text-xl font-bold text-accent-blue" id="val-status">--</div>
              </div>
            </div>
          </div>
        </div>
        
        <div class="text-base-600 text-sm italic">
          💡 <span id="math-hint">拖曳向量以觀察變化...</span>
        </div>
      </div>
    </div>

    <!-- AI 說明區 -->
    <div class="rounded-lg border border-base-600 bg-base-800/50 mt-6 p-6">
      <h3 class="text-xl font-bold text-base-50 mb-3">💡 為什麼這很重要？從幾何到 AI</h3>
      <p class="text-base-400 mb-4">
        向量內積不僅僅是高中數學課本裡的公式，它也是現代<strong class="text-base-50">人工智慧 (AI)</strong> 的核心基石。
      </p>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h5 class="text-accent-cyan font-bold mb-2">幾何意義：投影與方向</h5>
          <p class="text-sm text-base-600">
            內積的物理意義是一個向量在另一個向量上的<strong class="text-base-400">投影量</strong>乘以長度。
            透過開啟上方的「<strong class="text-base-400">鎖定單位長度 (Normalize)</strong>」，你可以消除長度的影響，純粹觀察兩個向量的<strong class="text-base-400">方向相似度</strong>。
            這就是數學上所謂的 <strong class="text-base-400">餘弦相似度 (Cosine Similarity)</strong>。
          </p>
        </div>
        <div>
          <h5 class="text-accent-yellow font-bold mb-2">AI 應用：Embeddings 與語意搜尋</h5>
          <p class="text-sm text-base-600">
            在大型語言模型 (ChatGPT, Gemini) 中，每一個字、每一句話都會被轉換成高維度的向量 (Embeddings)。
            電腦如何判斷「蘋果」跟「香蕉」很像，但跟「腳踏車」不像？
            靠的就是計算這些向量之間的<strong class="text-base-400">餘弦相似度</strong>！
          </p>
          <ul class="text-sm text-base-600 mt-2 list-disc list-inside">
            <li>夾角越小 (接近 0°) → 語意越接近</li>
            <li>夾角 90° (垂直) → 毫無關聯</li>
          </ul>
        </div>
      </div>
    </div>

    <div class="text-center mt-6">
      <a href="/math/" class="px-4 py-2 rounded border border-base-600 text-base-400 hover:bg-base-600 transition">← 返回數學工具</a>
    </div>
  </div>

  <script>
    import { VectorVisualizer } from '@/lib/math/VectorVisualizer';

    document.addEventListener('DOMContentLoaded', () => {
      const canvas = document.getElementById('vectorCanvas') as HTMLCanvasElement;
      
      const elDot = document.getElementById('val-dot');
      const elCos = document.getElementById('val-cos');
      const elDeg = document.getElementById('val-deg');
      const elStatus = document.getElementById('val-status');
      const elSimBar = document.getElementById('sim-bar');
      const elHint = document.getElementById('math-hint');
      const toggleNorm = document.getElementById('normalizeToggle') as HTMLInputElement;

      const viz = new VectorVisualizer('vectorCanvas', (state) => {
        if(elDot) elDot.innerText = state.dotProduct.toFixed(2);
        
        if(elCos) {
            const cos = state.cosineSimilarity;
            elCos.innerText = cos.toFixed(3);
            
            const pct = ((cos + 1) / 2) * 100;
            if (elSimBar) {
                elSimBar.style.width = `${pct}%`;
                if (cos > 0.5) {
                  elSimBar.className = 'h-full bg-accent-green transition-all duration-300';
                } else if (cos < -0.5) {
                  elSimBar.className = 'h-full bg-accent-red transition-all duration-300';
                } else {
                  elSimBar.className = 'h-full bg-accent-yellow transition-all duration-300';
                }
            }
        }

        if(elDeg) elDeg.innerText = `${state.angleDeg.toFixed(1)}°`;

        if(elStatus) {
            const cos = state.cosineSimilarity;
            let status = '';
            let hint = '';
            let statusColor = 'text-accent-blue';
            
            if (Math.abs(cos) < 0.05) {
                status = '正交 / 無關';
                statusColor = 'text-accent-yellow';
                hint = '向量互相垂直 (90°)，內積為 0，代表兩者毫不相關。';
            } else if (cos > 0.9) {
                status = '極度相似';
                statusColor = 'text-accent-green';
                hint = '方向幾乎一致，內積為正最大值。';
            } else if (cos < -0.9) {
                status = '完全相反';
                statusColor = 'text-accent-red';
                hint = '方向相反，內積為負最大值。';
            } else if (cos > 0) {
                status = '正相關';
                statusColor = 'text-accent-cyan';
                hint = '投影方向相同。';
            } else {
                status = '負相關';
                statusColor = 'text-accent-yellow';
                hint = '投影方向相反。';
            }
            
            elStatus.innerText = status;
            elStatus.className = `text-xl font-bold ${statusColor}`;
            if(elHint) elHint.innerText = hint;
        }
      });

      toggleNorm.addEventListener('change', (e) => {
        viz.setNormalizedMode((e.target as HTMLInputElement).checked);
      });

      document.querySelectorAll('.preset-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
           const type = (e.currentTarget as HTMLElement).dataset.type;
           viz.setVectors(type as any);
        });
      });
    });
  </script>
</BaseLayout>
